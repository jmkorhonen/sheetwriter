{
  "name": "sheetwriter",
  "private": true,
  "description": "A text editor whose document is an Excel workbook. Single HTML file; this package only runs the browser tests headlessly.",
  "scripts": {
    "test": "node tests/run.js",
    "build": "python build.py"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
